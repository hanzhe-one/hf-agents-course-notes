# Hugging Face Agents Course — 依赖 / Dependencies (Unit 1–3)
# 按需安装；examples 用到哪些就装哪些。

# Unit 1 & Unit 3 (SmolAgents) + HF serverless Inference
smolagents[toolkit]>=1.0
huggingface-hub>=0.23
python-dotenv>=1.0

# Unit 1 reAct.py 直接调 InferenceClient
# （huggingface-hub 已包含 InferenceClient）

# Unit 2 — LangGraph
langgraph>=0.2
langchain>=0.2
langchain-openai>=0.1

# Unit 2 — LlamaIndex
llama-index-core>=0.10

# Unit 3 — Alfred（数据集 + 检索）
datasets>=2.20
langchain-core>=0.2
langchain-community>=0.2

# 可选 / optional
# pandas>=2.0          # multiAgent.py 用到
# duckduckgo-search     # smolagents[toolkit] 已带 DuckDuckGoSearchTool
